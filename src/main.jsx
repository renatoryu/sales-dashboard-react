import React from "react";
import ReactDOM from "react-dom/client";

import ChartComponent from "./SalesChart.jsx"; 

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ChartComponent /> 
  </React.StrictMode>
);